# o - organization（组织-公司）
# ou - organization unit（组织单元-部门）
# c - countryName（国家）
# dc - domainComponent（域名）
# sn - suer name（真实名称）
# cn - common name（常用名称

spring:
  ldap:
    urls: ldap://ldap.jinweihaowu.com:1389
    base: dc=befriend,dc=com
    username: 'cn=Directory Manager'
    password: ENC(ZGq/N+XhylNDRX6o71hwREAgirs4sq5Qbm1cRITFART7jkIyzhwGuym0XW0yiAP1)

ldap:
  custom:  {
    # 用户 RDN Key uid | cn
    userId: cn,
    userBaseDN: ou=People,
    # 用户对象属性
    userObjectClass: inetOrgPerson,
    # 用户组 RDN Key uid | cn
    groupId: cn,
    groupBaseDN: ou=Groups,
    # 用户组对象属性
    groupObjectClass: groupOfUniqueNames,
    # 用户组成员属性
    groupMember: uniqueMember
  }